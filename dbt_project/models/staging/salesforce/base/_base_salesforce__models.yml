version: 2

models:
  - name: base__salesforce_rev_schedule
    columns:
      - name: sfdc_rev_schedule_id
        description: '{{ doc("sfdc_rev_schedule_id") }}'
        tests:
          - unique
          - not_null
      - name: sfdc_credit_note_item_id
        description: '{{ doc("sfdc_credit_note_item_id") }}'