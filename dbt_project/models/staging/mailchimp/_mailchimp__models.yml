version: 2

models:
  - name: stg_mailchimp__list_members
    columns:
      - name: list_member_id
        tests:
          - not_null
          - unique

  - name: stg_mailchimp__email_activity
    columns:
      - name: email_record_id
        tests:
          - not_null
          - unique

  - name: stg_mailchimp__list_segments
    columns:
      - name: list_segment_id
        tests:
          - not_null
          - unique
