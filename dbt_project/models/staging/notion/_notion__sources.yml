version: 2

sources:
  - name: notion
    description: '{{ doc("src_notion") }}'
    schema: notion
    database: raw
    loader: stitch_tap
    loaded_at_field: _sdc_batched_at
    freshness:
      warn_after:
        count: 2
        period: day
      error_after:
        count: 4
        period: day
    meta:
      team: ['Data']
      owner_name: Elize Papineau
    tables:
      - name: query_databases
        description: An extract of databases currated in Notion.
